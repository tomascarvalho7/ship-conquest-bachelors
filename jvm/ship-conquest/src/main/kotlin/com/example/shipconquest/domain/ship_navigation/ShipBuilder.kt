package com.example.shipconquest.domain.ship_navigation

/*
data class ShipBuilder(val sid: Int, val position: dynamicUtils, val event: Event?)

fun ShipBuilder.getPosition(instant: Instant): dynamicUtils {
    if (event == null || instant.isBefore(event.instant)) return position

    return event.details.buildPosition(position = position)
}
*/